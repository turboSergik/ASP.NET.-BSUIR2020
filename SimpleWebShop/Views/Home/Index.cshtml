@model HomeViewModel
<h3 class="mt-6">@ViewBag.Message</h3>

<div id="homePageDiv" class="row mt-5 mb-2">
    @{
        foreach (Car car in Model.favCars)
        {
            @Html.Partial("AllCars", car)
        }
    }
</div>
